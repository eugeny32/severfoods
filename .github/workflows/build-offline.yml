name: Build offline Windows installer

on:
  workflow_dispatch:
    inputs:
      commit_installer:
        description: 'Закоммитить готовый .exe в offline/dist/'
        type: boolean
        default: true

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: offline
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Build Windows installer
        run: npm run build

      - name: Read version
        id: pkg
        shell: bash
        run: echo "version=$(node -p "require('./package.json').version")" >> "$GITHUB_OUTPUT"

      - name: Commit installer into offline/dist
        if: ${{ inputs.commit_installer }}
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -B "${{ github.ref_name }}"
          # latest.yml/*.blockmap нужны electron-updater для автообновления —
          # без них старые клиенты не узнают о новой версии.
          git add dist/*.exe dist/*.yml dist/*.blockmap
          if git diff --cached --quiet; then
            echo "Нет изменений в dist/ — пропускаем коммит"
          else
            git commit -m "Собрать оффлайн-установщик v${{ steps.pkg.outputs.version }} [skip ci]"
            # См. тот же комментарий в build-android.yml: две сборки пишут в
            # одну ветку, и вторая получала отказ «fetch first».
            for attempt in 1 2 3; do
              if git push origin "HEAD:${{ github.ref_name }}"; then break; fi
              echo "Ветка изменилась, переносим коммит поверх (попытка $attempt)"
              git pull --rebase origin "${{ github.ref_name }}"
              sleep 5
            done
          fi

      # Артефакт — только копия для скачивания вручную. Его квота в GitHub
      # исчерпаема, и выпуск (коммит установщика в offline/dist) не должен
      # от неё зависеть — поэтому шаг идёт ПОСЛЕ коммита и не валит сборку.
      - name: Upload installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: SeverFoods-Setup-${{ steps.pkg.outputs.version }}
          path: |
            offline/dist/*.exe
            offline/dist/*.yml
            offline/dist/*.blockmap
          if-no-files-found: error
        continue-on-error: true
