{
  "name": "severfoods-android",
  "version": "1.7.13",
  "description": "Система учёта питания — СеверФудс (планшеты Android)",
  "author": "Palchenkov E.I. / OOO Sever",
  "license": "MIT",
  "private": true,
  "scripts": {
    "www": "bash build-www.sh",
    "sync": "npm run www && npx cap sync android",
    "build": "npm run sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.1",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
